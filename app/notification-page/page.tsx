import NotificationPage from "@/notification page/index";

export const metadata = {
  title: "Notifications & Advisory Hub | Smart Crop",
  description: "Critical risk alerts, weather warnings, officer updates, and scheme notifications.",
};

export default function NotificationPageRoute() {
  return <NotificationPage />;
}
